# Whisper API (transcription)
OPENAI_API_KEY=sk-...

# Gemini API (B-roll still images, highlight detection)
GEMINI_API_KEY=...

# Renoise (B-roll videos) — managed by the existing video-maker:renoise-gen skill
RENOISE_API_KEY=...

# Optional: override defaults
# WHISPER_MODEL=whisper-1
# GEMINI_MODEL=gemini-2.5-pro
